#include <bits/stdc++.h> 

long getMaximumProfit(long *arr, int n) {

    long profit=0;

    for(int i=1;i<n;i++)

    {

        if(arr[i]>= arr[i-1])

                profit = profit + (arr[i]-arr[i-1]);  

    }

    return profit;

}
